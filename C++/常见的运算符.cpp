C++ 中的运算符可以分为以下几类：

1. 算术运算符：用于执行基本算术操作，如加、减、乘、除等。常见的算术运算符有 `+`、`-`、`*`、`/`、`%`（取模）。

2. 关系运算符：用于比较两个变量的大小关系，返回结果为布尔类型 `true` 或 `false`。常见的关系运算符有 `<`、`<=`、`>`、`>=`、`==`、`!=`。

3. 逻辑运算符：用于组合多个条件表达式，判断表达式是否成立。常见的逻辑运算符有 `&&`（与）、`||`（或）、`!`（非）。

4. 位运算符：用于对二进制数进行位操作，如按位与、按位或、按位异或等。常见的位运算符有 `&`、`|`、`^`、`<<`（左移）、`>>`（右移）。

5. 赋值运算符：用于将右侧表达式的值赋给左侧变量。常见的赋值运算符有 `=`、`+=`、`-=`、`*=`、`/=`、`%=`、`&=`、`|=`、`^=`、`<<=`、`>>=`。

6. 条件运算符：也称为三目运算符，用于在两个表达式之间选择一个返回。语法为 `condition ? expression1 : expression2`。

7. 逗号运算符：用于在一条语句中执行多个表达式，并返回最后一个表达式的值。语法为 `expression1, expression2, ..., expressionN`。

8. 成员运算符：用于访问类、结构体和共同体等复合类型的成员。成员运算符分为点运算符 `.` 和箭头运算符 `->`。

9. 类型转换运算符：用于将一个类型的变量转换为另一个类型。类型转换运算符分为显示类型转换和隐式类型转换。

10. 其他运算符：包括地址运算符 `&` 和指针运算符 `*` 等。

以上是 C++ 中常见的运算符，如果您还需要了解更多的运算符，可以查看 C++ 参考手册。